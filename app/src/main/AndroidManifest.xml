<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahmoudabdelmoaz.deliverymotors">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme">
        <activity
            android:name=".Welcome"
            android:theme="@style/WelcomeTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StartActivity"
            android:theme="@style/StartTheme" />
        <activity android:name=".SignInEnterNumber" />
        <activity android:name=".SignInVerify" />
        <activity android:name=".SignInEnterEmail" />
        <activity android:name=".SignInPassword" />
        <activity android:name=".SignInUsername" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/MainTheme" />
        <activity android:name=".YourOrders"
            android:theme="@style/itemTheme"></activity>
    </application>

</manifest>